export function Header() {
  return (
    <header className="fixed top-0 left-0 right-0 z-50 bg-white border-b border-gray-200 py-2 px-4 flex items-center h-14">
      <h1 className="text-lg font-semibold text-gray-800">
        LangGraph チャット
      </h1>
    </header>
  );
}
